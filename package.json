{
  "name": "dayswithoutclaudeincident",
  "version": "1.0.0",
  "description": "Static site generator: days since the last Claude status incident.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "npm run build && cp package.json dist/package.json && (cd dist && npm install --omit=dev --no-audit --no-fund --no-package-lock) && (cd dist && zip -r ../lambda.zip .)"
  },
  "license": "ISC",
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
